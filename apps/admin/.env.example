# Nest API (local dev: match apps/api port, default 2005)
VITE_API_URL=http://127.0.0.1:2005

# Production (Render static site — set in dashboard, then redeploy admin):
# VITE_API_URL=https://api.loancompass.app
